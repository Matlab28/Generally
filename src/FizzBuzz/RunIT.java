package FizzBuzz;

import java.util.Scanner;

public class RunIT {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        New project = new New();
        FromZero zero = new FromZero();
        StillTrying trying = new StillTrying();
        trying.calculating(scanner);

//        zero.calculating(scanner);
//        project.calculating(scanner);
    }
}
